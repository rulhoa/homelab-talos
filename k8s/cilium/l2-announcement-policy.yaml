apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: loadbalancer-l2-policy
spec:
  loadBalancerIPs: true
  externalIPs: true
  # optionally restrict to a specific NIC, e.g. interfaces: ["^eth0$"]
  #interfaces:
  #- ^eth[0-9]+
  